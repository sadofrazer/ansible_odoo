all:
  vars:
    ansible_ssh_common_args: '-o StrictHostKeyChecking=no'

Prod:
  hosts:
    client:
      ansible_host: localhost
      db_qty: 1
      ansible_connection: local
      ansible_sudo_pass: "definir"